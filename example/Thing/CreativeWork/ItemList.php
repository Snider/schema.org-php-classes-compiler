<?php

class Thing_CreativeWork_ItemList extends Thing_CreativeWork{

             /**
              * A single list item.
              *
              * @var $itemListElement string
              */
              public $itemListElement;

             /**
              * Type of ordering (e.g. Ascending, Descending, Unordered).
              *
              * @var $itemListOrder string
              */
              public $itemListOrder;
}